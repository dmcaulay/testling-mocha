{
  "name": "testling-mocha",
  "version": "0.0.0",
  "description": "A sample project that uses testling and mocha to run tests in the browser",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "Dan McAulay",
  "license": "BSD",
  "dependencies": {
    "shoe": "0.0.7"
  }
}
